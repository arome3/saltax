services:
  saltax:
    build: .
    ports:
      - "8080:8080"
      - "8081:8081"    # TS proxy internal bridge (dev)
    env_file: .env
    volumes:
      - ./src:/app/src        # Hot reload (dev only)
      - ./rules:/app/rules
    restart: unless-stopped
